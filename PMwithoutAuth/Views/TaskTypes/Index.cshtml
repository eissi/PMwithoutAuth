@model IEnumerable<PMwithoutAuth.TaskType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaskTypeName)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TaskTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskTypeName }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskTypeName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskTypeName })
        </td>
    </tr>
}

</table>
